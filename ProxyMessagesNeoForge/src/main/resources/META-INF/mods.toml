modLoader = "javafml"
loaderVersion = "[47,)"
license = "All Rights Reserved"

[[mods]]
modId = "proxymessages"
version = "${version}"
displayName = "ProxyMessages"
description = "NeoForge companion mod for the ProxyMessages Velocity plugin."

# Register the Mixin config so SpongeMixin processes it at launch.
[[mixins]]
config = "mixins.proxymessages.json"

[[dependencies.proxymessages]]
    modId = "neoforge"
    mandatory = true
    versionRange = "[47,)"
    ordering = "NONE"
    side = "SERVER"

[[dependencies.proxymessages]]
    modId = "minecraft"
    mandatory = true
    versionRange = "[1.20.1,1.21)"
    ordering = "NONE"
    side = "SERVER"
